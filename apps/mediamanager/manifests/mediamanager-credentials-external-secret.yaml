---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: mediamanager-credentials
  namespace: mediamanager
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: azure-keyvault
    kind: ClusterSecretStore
  target:
    name: mediamanager-credentials
    creationPolicy: Owner
  data:
    - secretKey: username
      remoteRef:
        key: mediamanager-user
    - secretKey: password
      remoteRef:
        key: mediamanager-password
    - secretKey: db_hostname
      remoteRef:
        key: mediamanager-db-hostname
    - secretKey: db_port
      remoteRef:
        key: mediamanager-db-port
    - secretKey: db_name
      remoteRef:
        key: mediamanager-db-name
    - secretKey: oidc_client_secret
      remoteRef:
        key: argocd-oidc-client-secret
    - secretKey: admin_email
      remoteRef:
        key: admin-email
    - secretKey: smtp_host
      remoteRef:
        key: smtp-server
    - secretKey: smtp_port
      remoteRef:
        key: smtp-port
    - secretKey: smtp_user
      remoteRef:
        key: smtp-login
    - secretKey: smtp_password
      remoteRef:
        key: mediamanager-smtp-key
    - secretKey: smtp_from_email
      remoteRef:
        key: mediamanager-email
