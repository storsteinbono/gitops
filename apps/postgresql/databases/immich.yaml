apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: immich
spec:
  name: immich
  owner: immich
  cluster:
    name: postgresql
  extensions:
    - name: vchord
    - name: earthdistance
      ensure: present