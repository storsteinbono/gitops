apiVersion: v1
kind: Service
metadata:
  name: openhands
  namespace: openhands
  labels:
    app: openhands
spec:
  ports:
    - port: 3000
      targetPort: 3000
      name: http
  selector:
    app: openhands
