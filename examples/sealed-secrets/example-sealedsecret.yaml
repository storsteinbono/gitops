# Example SealedSecret (SAFE TO COMMIT TO GIT!)
# This is encrypted and can only be decrypted by the controller in your cluster
---
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: my-app-secret
  namespace: default
  annotations:
    sealedsecrets.bitnami.com/cluster-wide: "false"
    sealedsecrets.bitnami.com/namespace-wide: "false"
spec:
  encryptedData:
    # These are example encrypted values - replace with your own!
    # Generated using: kubeseal --format=yaml < example-secret.yaml > example-sealedsecret.yaml
    database-url: AgBQ7Vn8kF... # Encrypted value
    api-key: AgCT9Kx2mE... # Encrypted value
    username: AgDX4Zp1wR... # Encrypted value
    password: AgEY8Lk3nT... # Encrypted value
  template:
    metadata:
      name: my-app-secret
      namespace: default
    type: Opaque

# NOTE: The actual encrypted data above is just a placeholder.
# You need to generate real encrypted values using kubeseal CLI.
# See SEALED_SECRETS.md for instructions.
