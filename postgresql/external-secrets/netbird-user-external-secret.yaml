---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: netbird-user
  namespace: postgres
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: azure-keyvault
    kind: ClusterSecretStore
  target:
    name: netbird-user
    creationPolicy: Owner
    template:
      metadata:
        labels:
          cnpg.io/reload: "true"
  data:
    - secretKey: username
      remoteRef:
        key: postgres-netbird-username
    - secretKey: password
      remoteRef:
        key: postgres-netbird-password
