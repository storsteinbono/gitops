---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: authentik-user
  namespace: postgres
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: azure-keyvault
    kind: ClusterSecretStore
  target:
    name: authentik-user
    creationPolicy: Owner
    template:
      metadata:
        labels:
          cnpg.io/reload: "true"
  data:
    - secretKey: username
      remoteRef:
        key: postgres-authentik-username
    - secretKey: password
      remoteRef:
        key: postgres-authentik-password
