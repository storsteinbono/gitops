apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# PostgreSQL database cluster and provisioned databases

# Note: bootstrap/postgresql.yaml is managed directly by root app, not through Kustomize
# This component is a placeholder for future PostgreSQL-specific patches or resources
resources: []

labels:
  - pairs:
      component: database
      tier: postgres
