apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# GPU support components - for NVIDIA GPU workloads

resources:
  - ../../../infrastructure/nvidia-device-plugin.yaml
  - ../../../infrastructure/nvidia-runtime.yaml

labels:
  - pairs:
      component: infrastructure
      tier: gpu
      feature: nvidia
