apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# Harbor container registry - optional infrastructure
# NOTE: Harbor is not currently deployed in this cluster

# resources:
#   - ../../../infrastructure/harbor/harbor.yaml

labels:
  - pairs:
      component: infrastructure
      tier: registry
      feature: harbor
