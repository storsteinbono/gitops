---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: authentik
  namespace: authentik
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`auth.torsteinbo.net`)
      kind: Rule
      services:
        - name: authentik-server
          port: 80
